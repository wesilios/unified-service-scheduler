# Unified Service Scheduler — Task Tracker

Tracks progress across the 4 top-level tasks defined in `Agent.md`. Update status
and add checkpoint notes as work completes so work can resume cleanly across sessions — this
file is the shared state between them, read at the start of each new one.

Status legend: `TODO` / `IN PROGRESS` / `BLOCKED` / `DONE`

---

## Task 1 — System Design Document (`architecture.md`)

Status: **DONE** — all 17 sub-items complete; 1 follow-up doc-simplification item open (see 1.18)

| # | Item | Status | Notes |
|---|------|--------|-------|
| 1.1 | Problem statement | DONE | |
| 1.2 | Domain assumptions | DONE | Revised: Technician/ServiceBay now external systems (validated via `ITechnicianService`/`IServiceBayService`, mocked, Refit `I*HttpClient` defined but unwired); ServiceType JSON-backed via `IServiceTypeProvider`; Vehicle is free text `"Make - Model - Trim/Variant+Year"`, no Vehicle entity |
| 1.3 | C4 L1 — System Context diagram | DONE | Two actors, two API surfaces (Customer Booking API implemented; Staff/Admin API documented, not implemented); 3 external systems (Technician, Service Bay, Notification), all mocked |
| 1.4 | C4 L2 — Container diagram | DONE | Scheduler API shows split Customer Booking API / Staff-Admin API (placeholder) internally; app/domain/infra layers moved to L3 (not duplicated here); Postgres reduced to `Appointments` only |
| 1.5 | C4 L3 — Component diagram | DONE | Rendered as Mermaid, split into 3 diagrams (Request Handling, Domain Model, Infrastructure & External Integrations) for readability. Does not reflect the Customer/Staff API split, per user. Introduces `AppointmentSchedulingPolicy` domain service name — later implemented under that exact name (`Scheduler.Domain/Services/AppointmentSchedulingPolicy.cs`) |
| 1.6 | C4 L4 — Code diagram | DONE | Split into L4a (Handler & MockService injection, incl. Dictionary-backed `IServiceTypeProvider`) and L4b (Domain model detail, incl. `AppointmentStatus` enum). Does not reflect the API surface split |
| 1.7 | Data model | DONE | `Appointment` + new `AppointmentSlot` ledger table (concurrency guarantee); `Status` stored as string via EF `HasConversion<string>()`; ServiceType seed catalog documented in Domain Assumptions |
| 1.8 | Data flow explanation (mermaid) | DONE | Sequence diagram for `CreateAppointmentCommand` incl. all failure branches (400/409) and the TOCTOU-race explanation for why the DB constraint, not the read-check, is the real concurrency guarantee |
| 1.9 | Observability | DONE | Serilog + OTel exporter (vendor-agnostic backend); domain metrics (booking outcome rate, 409-conflict rate, external mock-service latency, availability-check latency); tracing mapped to Data Flow stages; correlation ids; health checks |
| 1.10 | Security (authN/authZ assumptions) | DONE | **Revised**: Customer Booking API is now documented as *intentionally* unauthenticated (guest checkout via Email+Phone, per the Customer redesign) rather than `sub`-scoped — the JWT/`sub`-claim design moved to a "Future: once login is introduced" subsection. Honestly flags the consequence (no verification that a caller owns the Email+Phone they supply). Staff/Admin API section unchanged |
| 1.11 | Technology choices | DONE | One-line WHY per technology. *(Originally flagged FluentValidation/OpenTelemetry/Serilog as not yet wired into any `.csproj` — all three were wired in during Task 2 and are confirmed present in `Scheduler.Application`/`Scheduler.Api`'s `.csproj` files as of this note.)* |
| 1.12 | Testing strategy (Unit + Integration) | DONE | §9. Unit: Domain/Application/Infrastructure, >80% target, Moq (not FluentAssertions — flagged its 2025 commercial license change), avoid re-testing branch logic at integration level. Integration: WebApplicationFactory + real SQLite, concurrency race test is the key one, operating-hours boundary via real HTTP |
| 1.13 | Future evolution — concurrency strategy | DONE | §10. 3 scenarios (retry/backoff UX, per-resource serialization for hot rows, multi-region out-of-scope note), all gated on 409-rate/lock-wait metrics, not preemptive |
| 1.14 | Future evolution — cache strategy | DONE | §5 finished: what's cached (availability + ServiceType catalog, not external-id validation), explicit invalidation + TTL backstop, safe-by-construction staleness, Redis trigger tied to horizontal scaling + §7's 409-rate metric |
| 1.15 | Future evolution — scalability strategy | DONE | §10. API horizontal scale-out (blocked on Redis per §5), DB read replicas (stale-read safety argument mirrors §5's cache-staleness argument), `DealershipId` partitioning, service-extraction path via existing `IDispatcher` boundary |
| 1.16 | Future evolution — production capacity triggers | DONE | §10 table: metric → threshold → action, referencing §7's metrics directly (409 rate, API CPU/mem, availability-check p95, external call latency, lock-wait time, per-dealership volume) |
| 1.17 | Future evolution — reliability | DONE | §10. Transactional atomicity (already in place), notification as best-effort/outbox pattern, Polly circuit breaker for future real HTTP clients, fail-closed on external validation failure, idempotency-under-retry analysis (accidental via AppointmentSlot, optional Idempotency-Key header as refinement) |
| 1.18 | Reduce C4 Level 4 (Code) diagram duplication | TODO | L4a/L4b (class diagrams) mostly restate what L3b's Domain Model component diagram, §6 Data Model's ER diagram, and Domain Clarifications' prose already say. Recommendation: drop L4 entirely, or fold its one genuinely new fact (`IServiceTypeProvider`'s `Dictionary` lookup being O(1), not a linear scan) into §6 Data Model as a one-line note; merge L4b's `Appointment`/`TimeRange`/`ServiceType`/`Dealership` class diagram into the Data Model section rather than showing the same entities twice (once as a class diagram, once as an ER diagram). Raised during a doc-simplification review alongside two other cuts — folding "Key Trade-offs" into Architecture Principles, and condensing Security's Abuse Mitigation subsection into a table — both of which are already applied; this is the remaining piece, deferred for user review before cutting content |

**Checkpoint / output:** —

**Follow-up pass — correlation-id documentation + Assessment Scope Notes:** added a new centralized "Assessment Scope Notes" section (right after Core Requirements, before Domain Clarifications) collecting every standing scope limitation as its own linkable subsection — mocked external systems, Service Type as a static file, Staff/Admin API not implemented, no auth, SQLite vs SQL Server, no API gateway/load balancer, no observability backend, no real Azure environment, single-instance deployment. Went through the ~35 scattered "this assessment"/"not implemented"/"out of scope" mentions found via grep and converted the duplicative ones into short phrase + link back to the canonical explanation, while leaving entity-specific context (e.g. the Service Bay/Technician `Mock*`/Refit implementation details) intact rather than stripping it down to just a link. Rewrote §7 Observability's "Correlation IDs" subsection, which had gone stale (it described `TraceIdentifier` but the actual code in `CorrelationIdMiddlewareExtensions` does capture-or-generate against `X-Correlation-Id`) — now documents the real behavior plus the architectural rationale (user-provided): a gateway/load balancer is the first hop, so honoring its correlation id (rather than always minting a fresh one) is what lets a trace follow a request from that edge component down through downstream services. Explicitly notes this assessment has no such component in front, so the auto-generate branch is what fires for real traffic here — the capture branch is demonstrated deliberately, not accidentally, via `Scheduler.Api.http` and two new integration tests.

---

## Task 2 — Implementation (Clean Architecture, per code diagram)

Status: **DONE** for the assessment's core surface — booking flow, availability query, guest-checkout customer matching, MVC Controllers, Scalar UI, and full Serilog/OpenTelemetry wiring all implemented and live-verified, including the concurrency guarantee under real parallel load (now also covered by an automated test, see Task 3). Remaining: a couple of flagged minor items (mock "not found" branch unreachable, no availability-cache read-through yet) — neither blocking.

### 2.0 Prerequisite fixes
| # | Item | Status | Notes |
|---|------|--------|-------|
| 2.0.1 | Resolve `SharedKernel.*` namespace in `Scheduler.Application` (Commands/Handlers/Interfaces/Queries/Dispatcher.cs) | DONE | Renamed to `Scheduler.Application.*` in place (not a separate `SharedKernel` project — no cross-project sharing need exists yet). Also collapsed the stray `Handlers.Abstractions` sub-namespace into plain `Handlers` for consistency with `ICommandHandler` |
| 2.0.2 | Rename `Appoinment.cs` → `Appointment.cs` | DONE | Filename only; class body unchanged (still empty — populating it is 2.1.1) |
| 2.0.3 | **Discovered while fixing 2.0.1**: `Scheduler.Application.csproj` referenced `Scheduler.Infrastructure` — backwards for Clean Architecture (Infrastructure implements Application's interfaces, so the dependency must point the other way; the old direction would've become circular the moment Infrastructure needed to reference Application back) | DONE | Removed Application→Infrastructure reference; added Infrastructure→Application reference. Added `Microsoft.Extensions.DependencyInjection.Abstractions` package directly to `Scheduler.Application.csproj` since `Dispatcher.cs`'s `GetRequiredService` call had been relying on that package leaking in transitively through the wrong Infrastructure reference. Full solution build verified green after the fix |

### 2.1 Scheduler.Domain
| # | Item | Status | Notes |
|---|------|--------|-------|
| 2.1.1 | `Appointment` entity (currently empty) | DONE | Populated: `Id`, `CustomerId`, `DealershipId`, `Vehicle`, `ServiceTypeCode`, `TechnicianId`, `ServiceBayId`, `Duration` (`TimeRange`), `Status`, `CreatedAt`, `Slots`. Private setters + `Create(...)` static factory (validates non-empty Vehicle/ServiceTypeCode, generates `AppointmentSlot` rows) |
| 2.1.2 | ~~`Technician` entity~~ | DROPPED | Now external; see `ITechnicianService` in 2.3 |
| 2.1.3 | ~~`ServiceBay` entity~~ | DROPPED | Now external; see `IServiceBayService` in 2.3 |
| 2.1.4 | `ServiceType` value/reference (name + duration) | DONE | Created as `Scheduler.Domain/ServiceType.cs` — minimal record (`Code`, `Description`, `Duration`), no behavior yet. Sourced via `IServiceTypeProvider`, not a DB entity |
| 2.1.5 | ~~`Vehicle` entity~~ | DROPPED | Free-text field on `Appointment`: `"Make - Model - Trim/Variant+Year"` |
| 2.1.6 | `Dealership` entity | DONE | `Id`, `Name`, `OperatingHoursStart`/`End` (`TimeOnly`). Seeded via EF `HasData` (single "Downtown Dealership", 08:00-17:00) since there's no admin API to create one |
| 2.1.7 | `Customer` entity | DONE | `Id`, `Name`, `Email`, `Phone`. **Redesigned to guest checkout**: no login required to book; `Email`+`Phone` together are the matching key — an existing match is reused, no match creates a new `Customer` (`CreateAppointmentCommandHandler.ResolveCustomerAsync`). `CustomerConflictException` (`Scheduler.Domain`) added for the race case, mirroring `AppointmentConflictException`. **Verified live**: two bookings with the same Email+Phone resolved to the same `CustomerId` |
| 2.1.8 | Value objects (e.g. time range/availability window) | DONE | `TimeRange` (`Start`/`End`, guards `End > Start`, `Overlaps(TimeRange)`) — now fully implemented, using the previously-unused `ValueObject` base |
| 2.1.9 | Domain services / invariants for availability + double-booking rules (`AppointmentSchedulingPolicy`) | DONE | Static (pure, no DI needed): `IsWithinOperatingHours` (rejects Sunday, cross-midnight, outside hours) + `HasNoOverlap`. Verified live via the smoke test below |
| 2.1.10 | Repository interfaces (`IAppointmentRepository`, etc.) | DONE | Created in `Scheduler.Application/Interfaces/` (not `Scheduler.Domain`) — matches architecture.md's C4 L3a, which places all repository/external-service interfaces in the Application layer, implemented by Infrastructure. This row's original placement under 2.1 (Domain) was a tracker inconsistency, not the actual design |
| 2.1.11 | `AppointmentSlot` concept (15-min slot ledger, one row per resource per slot) | DONE | `ResourceKind` enum (Technician/ServiceBay) + `AppointmentSlot` entity, generated via `Appointment.Create`'s ceiling-division slot expansion. `AppointmentConflictException` added (`Scheduler.Domain`) for the repository to signal a lost race. **Verified live**: two genuinely concurrent identical booking requests → exactly one 201, one 409 via the real DB unique constraint, not just the read-check |

### 2.2 Scheduler.Application
| # | Item | Status | Notes |
|---|------|--------|-------|
| 2.2.1 | `CreateAppointmentCommand` + handler (external validation + local availability check) | DONE | **Redesigned twice this session.** Shape: `CustomerName`/`CustomerEmail`/`CustomerPhone` replace `CustomerId` (guest checkout, no login). Logic: validation/availability steps extracted into shared `AppointmentAvailabilityChecker` (`Scheduler.Application/Services/`) so `CreateAppointmentCommandHandler` and the new `CheckAvailabilityQueryHandler` don't duplicate the ServiceType/Technician/ServiceBay/Dealership/operating-hours/overlap logic. Handler now: availability check → resolve-or-create `Customer` → insert (catches `AppointmentConflictException` → 409) → cache invalidation → mocked notification. Instrumented with `SchedulerInstrumentation` spans/metrics throughout |
| 2.2.2 | Availability query/use case | DONE | `CheckAvailabilityQuery`/`AvailabilityResult`/`AvailabilityStatus` + `CheckAvailabilityQueryHandler`, backed by the shared `AppointmentAvailabilityChecker`. Exposed via `GET /appointments/availability`. **Not** cache-backed yet — reads go straight to `IAppointmentRepository`, same as the command's own check; wiring `IAvailabilityCache`'s read side is still open (it currently only supports `InvalidateAsync`) |
| 2.2.3 | FluentValidation validators for commands | DONE | `CreateAppointmentCommandValidator` (updated for new Name/Email/Phone fields, `.EmailAddress()` rule added) + `CheckAvailabilityQueryValidator` (new) |
| 2.2.4 | DI registration for Application layer | DONE | `AddApplicationServices()` — registers `IDispatcher`, `AppointmentAvailabilityChecker`, both handlers, and FluentValidation validators via `AddValidatorsFromAssemblyContaining` |
| 2.2.5 | Interfaces implemented by Infrastructure: `IAppointmentRepository`, `ITechnicianService`, `IServiceBayService`, `IServiceTypeProvider`, `INotificationService`, `IAvailabilityCache`, `IDealershipRepository`, `ICustomerRepository` | DONE | Created in `Scheduler.Application/Interfaces/`, signatures match architecture.md C4 L4a (plus `IDealershipRepository`/`ICustomerRepository`, small necessary additions not in the original 6-interface list). `CancellationToken` params added throughout. `IServiceTypeProvider.TryGet` is synchronous — Dictionary lookup, no I/O |
| 2.2.6 | Observability instrumentation (`ActivitySource`/`Meter`) | DONE | `SchedulerInstrumentation` (`Scheduler.Application/Observability/`) — pure BCL (`System.Diagnostics`), no package needed at this layer. Spans mapped to Data Flow stages, metrics per architecture.md §7 (booking outcomes, 409-conflict rate, external-validation latency, availability-check latency). **Verified live**: custom `CreateAppointment` span correctly nested under the ASP.NET Core auto-instrumented HTTP span in console-exported traces |

### 2.3 Scheduler.Infrastructure
| # | Item | Status | Notes |
|---|------|--------|-------|
| 2.3.1 | `DbSet`s + entity configurations in `SchedulerDbContext` | DONE | `Appointment` (owns `TimeRange` as `StartTime`/`EndTime` columns via `OwnsOne`; `Status` → string conversion), `AppointmentSlot` (`ResourceKind` → string conversion; **composite `UNIQUE(ResourceKind, ResourceId, SlotStart)` index — the concurrency guarantee**), `Dealership` (seeded), `Customer` (**`UNIQUE(Email, Phone)` index added — the guest-checkout dedup guarantee, same pattern as `AppointmentSlot`**) |
| 2.3.2 | Repository implementations | DONE | `AppointmentRepository` (`GetOverlappingAsync` queries `Appointment` directly, not `AppointmentSlot` — matches Data Flow; `AddAsync` catches `DbUpdateException` → `AppointmentConflictException`), `DealershipRepository`, `CustomerRepository` (`FindByEmailAndPhoneAsync`, `AddAsync` catches `DbUpdateException` → `CustomerConflictException`) |
| 2.3.3 | Fix `AddInfrastructureServices` (currently `private`, unreachable) + implement registrations | DONE | Now `public`. Also switched `AddDbContextFactory` → `AddDbContext` (repositories inject `SchedulerDbContext` directly, not a factory — the prior registration wouldn't have resolved). Registers all 6 interfaces + `IDealershipRepository` |
| 2.3.4 | EF Core migrations + SQLite provider for this assessment (SQL Server is the production target) | DONE | `InitialCreate` migration generated (`dotnet ef migrations add`), applied via `Database.Migrate()` at startup (not `EnsureCreated`) — needed adding `Microsoft.EntityFrameworkCore.Design` to `Scheduler.Api.csproj` for the tooling to resolve the startup project |
| 2.3.5 | Cache abstraction (`IMemoryCache` now, Redis-ready per architecture) | DONE | `MemoryAvailabilityCache` — `InvalidateAsync` only (no read-through yet, since 2.2.2's availability query isn't built); key convention `availability:{resourceId}` documented for whenever that query starts populating it |
| 2.3.6 | `IServiceBayService` → `MockServiceBayService` (active); `IServiceBayHttpClient` (Refit) defined but left empty, DI registration commented out | DONE | `MockServiceBayService.ExistsAsync` treats `Guid.Empty` as not-found, anything else as valid — but `Guid.Empty` is also rejected upstream by `CreateAppointmentCommandValidator`'s `NotEmpty` rule, so the mock's "not found" branch is currently unreachable through the API. Minor inconsistency, not a bug — flagged for whoever builds richer mock data later |
| 2.3.7 | `ITechnicianService` → `MockTechnicianService` (active); `ITechnicianHttpClient` (Refit) defined but left empty, DI registration commented out | DONE | Same shape/caveat as 2.3.6 |
| 2.3.8 | `IServiceTypeProvider` backed by static JSON file loaded at startup | DONE | `JsonServiceTypeProvider` loads `Data/servicetypes.json` (the 6-entry catalog from Domain Assumptions) into a `Dictionary<string, ServiceType>` at construction; registered as a singleton, path resolved via `AppContext.BaseDirectory` |

### 2.4 Scheduler.Api
| # | Item | Status | Notes |
|---|------|--------|-------|
| 2.4.1 | Remove template weather-forecast endpoint | DONE | |
| 2.4.2 | Appointment endpoints (create, check availability, etc.) | DONE | **Refactored from Minimal API to MVC Controllers.** `Controllers/AppointmentsController.cs` — `POST /appointments` and `GET /appointments/availability`, both validate-then-dispatch-then-map-status. `Program.cs` no longer has inline route lambdas |
| 2.4.3 | Wire DI for Application + Infrastructure | DONE | `AddApplicationServices()` + `AddInfrastructureServices(configuration)` in `Program.cs`. Added `Scheduler.Api → Scheduler.Infrastructure` project reference (composition root needs it for DI wiring — this is the one place that's allowed to reference Infrastructure directly) |
| 2.4.4 | Swagger/OpenAPI config | DONE | Built-in `AddOpenApi()`/`MapOpenApi()` + **Scalar** (`Scalar.AspNetCore`) for interactive docs at `/scalar/v1` — chosen over Swashbuckle to avoid a second competing OpenAPI generator alongside the built-in one. `Scheduler.Api.http` updated with real requests covering every endpoint/branch (see note below on `.http` for E2E) |
| 2.4.5 | Global error handling / problem details | DONE | `AddProblemDetails()` + `UseExceptionHandler()` — unhandled exceptions now return RFC 9457 ProblemDetails instead of a raw 500. Known result branches (400/409) are handled explicitly in the controller; FluentValidation failures return `ValidationProblem` |
| 2.4.6 | Observability wiring (OpenTelemetry) | DONE | Full Serilog + OpenTelemetry: `SchedulerInstrumentation` spans/metrics (2.2.6) wired to console exporters; correlation-id middleware (`Middleware/CorrelationIdMiddlewareExtensions.cs`) pushes `CorrelationId`+`TraceId` into the Serilog `LogContext`; `/health` liveness endpoint. **Verified live**: custom spans nested correctly under ASP.NET Core's auto-instrumented HTTP spans; log lines carry both ids; noisy `Microsoft.AspNetCore`/`EntityFrameworkCore` categories suppressed so business-level logs aren't buried |
| 2.4.7 | Serilog config extracted to `serilog.json` | DONE | Sinks (Console with a custom `Corr:{CorrelationId} Trace:{TraceId}` output template, OpenTelemetry), `MinimumLevel.Override`, and enrichers all live in `serilog.json`, loaded via `builder.Configuration.AddJsonFile(...)` + `.ReadFrom.Configuration(...)`. `Program.cs` no longer hardcodes any of this |
| 2.4.8 | Keep `Program.cs` thin | DONE | OpenTelemetry SDK setup → `Extensions/ObservabilityServiceCollectionExtensions.cs` (`AddSchedulerObservability`). Correlation-id middleware → `Middleware/CorrelationIdMiddlewareExtensions.cs` (`UseCorrelationId`). Endpoints → `AppointmentsController`. `Program.cs` is now ~65 lines of composition-root wiring only |

**Checkpoint / output:** Task 2 core surface is functionally complete and live-verified end-to-end, including Controllers, Scalar UI, full Serilog/OpenTelemetry, and `serilog.json` externalization. Full solution builds clean (`dotnet build`, 0 errors).

**On `Scheduler.Api.http` for E2E testing** (question raised mid-session): good for manual/exploratory testing and living documentation — any teammate can fire a request straight from the IDE without writing code. Not a substitute for automated CI coverage: not easily assertable, and running it unattended needs extra tooling (e.g. JetBrains' `ijhttp` CLI) that isn't part of this stack. Kept as the manual reference; **Task 3's `Scheduler.IntegrationTests`** (below) is the real automated E2E layer, per the strategy already in architecture.md §9. *(Superseded — `Scheduler.Api.http` itself was later removed in favor of a Postman collection + a standalone Node.js concurrency script; see the last follow-up pass under Task 4. The reasoning above about manual testing's role relative to `Scheduler.IntegrationTests` still holds, just via different tools.)*

**Live verification performed this session** (ran the API with `dotnet run`, real SQLite DB, real HTTP requests via `curl`, then again via the automated integration test suite below):
- Happy path: valid booking → `201 Created`, correct duration (service-type-driven), correct `AppointmentSlot` rows (2 resources × N 15-min increments)
- Double-booking via read-check: same slot requested again → `409 Conflict`
- Outside operating hours (07:00, before 08:00 open) and Sunday: → `400 Bad Request`
- Empty-GUID technician: caught by FluentValidation before reaching the mock service → `400` (validation problem details)
- **Genuine concurrency race**: N identical booking requests fired truly in parallel → exactly one `201`, the rest `409` — confirms the `AppointmentSlot` unique constraint (not the read-check) is what actually enforces the concurrency-safety requirement from Agent.md's core requirement #3. Verified twice: manually via parallel `curl`, and automated via `Task.WhenAll` in `Scheduler.IntegrationTests`
- Same-customer guest-checkout matching: two bookings with the same Email+Phone resolve to the same `CustomerId`
- `/health` → `200 Healthy`; `/scalar/v1` and `/openapi/v1.json` → `200`
- Startup applies the `InitialCreate` EF Core migration and the seeded `Dealership` row correctly

**Remaining minor items:**
- 2.2.2's availability check is not cache-backed yet (`IAvailabilityCache` only supports `InvalidateAsync`)
- **2.3.6/2.3.7 mock "not found" branch is unreachable** given current validation (`Guid.Empty` rejected upstream by FluentValidation) — worth deciding whether to loosen validation or accept as-is when richer mock data is built
- `SharedKernel.*`-adjacent open decision (2.0.1) is resolved; no other open decisions carried from Task 2

### 2.5 Code organization (requested cleanup pass)

| # | Item | Status | Notes |
|---|------|--------|-------|
| 2.5.1 | `Scheduler.Domain` grouped into category folders | DONE | `Entities/` (Appointment, AppointmentSlot, AppointmentStatus, Customer, Dealership, `IAggregateRoot`, ResourceKind), `ValueObjects/` (ValueObject base, TimeRange, ServiceType), `Exceptions/` (AppointmentConflictException, CustomerConflictException), `Services/` (AppointmentSchedulingPolicy). Domain project root now has zero loose `.cs` files. `ServiceType` and `AppointmentStatus`/`IAggregateRoot` weren't explicitly named in the request — classified by fit (ServiceType has record value-equality → ValueObjects; the enum/marker interface are Appointment-specific → Entities) and called out here rather than silently decided |
| 2.5.2 | `SchedulerDbContext.OnModelCreating` split into per-entity config classes | DONE | `DataAccess/Configurations/{Appointment,AppointmentSlot,Dealership,Customer}Configuration.cs`, each an `IEntityTypeConfiguration<T>`. `OnModelCreating` is now one line: `modelBuilder.ApplyConfigurationsFromAssembly(typeof(SchedulerDbContext).Assembly)` — a new entity's configuration file is picked up automatically, no edit to `SchedulerDbContext` needed |
| 2.5.3 | Migrations moved to `DataAccess/Migrations/` | DONE | Regenerated (`ef migrations remove` + `ef migrations add --output-dir DataAccess/Migrations`) rather than hand-editing generated files — correct namespace (`Scheduler.Infrastructure.DataAccess.Migrations`) came for free. README.md's new "Database Migrations" subsection documents the required `--output-dir` flag for future migrations, since `dotnet ef` doesn't infer location from existing ones |
| 2.5.4 | **Discovered while verifying 2.5.3**: `WebApplicationFactory` + `Database.Migrate()` called from `Program.cs`'s startup path is unsafe — intermittently threw `SQLite Error 1: table "Appointments" already exists` | DONE | Root cause: `WebApplicationFactory`'s test-host machinery (`DeferredHostBuilder`) does not guarantee `Program.cs`'s startup path executes in a way that's safe for a non-idempotent schema migration. Fix: `Program.cs` skips its own auto-migrate under `IsEnvironment("Testing")`; `SchedulerApiFactory` (Scheduler.IntegrationTests) instead applies the migration itself via a **standalone `SchedulerDbContext`** built directly with `DbContextOptionsBuilder`, in its constructor, before `CreateClient()`/the DI-resolved host is ever touched. Verified stable across repeated full-suite runs after the fix. This is a real, non-obvious finding worth remembering if `Program.cs`'s startup logic changes again |

---

## Task 3 — Tests

Status: **DONE** — 77 tests passing (64 unit + 13 integration), including the automated concurrency-race test; HTML coverage reporting wired up locally and in CI (3.7)

| # | Item | Status | Notes |
|---|------|--------|-------|
| 3.1 | Create `Scheduler.IntegrationTests` project + add to `.sln` | DONE | `Microsoft.AspNetCore.Mvc.Testing` + xUnit. `Program` exposed via `public partial class Program;` at the bottom of `Program.cs` (top-level-statements `Program` is otherwise implicitly internal, invisible to `WebApplicationFactory<Program>`) |
| 3.2 | Remove default `UnitTest1.cs` stub | DONE | |
| 3.3 | Unit tests — Domain | DONE | `TimeRangeTests` (validation, `Overlaps` incl. adjacency edge cases, equality), `AppointmentSchedulingPolicyTests` (operating-hours boundaries, Sunday, cross-midnight, overlap), `AppointmentTests` (`Create` validation, slot-count generation for 30/45/60-min durations) — 26 tests |
| 3.4 | Unit tests — Application | DONE | `AppointmentAvailabilityCheckerTests` (every `AvailabilityStatus` branch), `CreateAppointmentCommandHandlerTests` (new/existing/racing customer, every failure branch, insert-conflict → 409, notification/cache verified via `Moq.Verify`), `CheckAvailabilityQueryHandlerTests`, both validators — 33 tests. Moq used for all 8 interfaces; `AppointmentAvailabilityChecker` itself is composed with mocked dependencies rather than mocked directly (not designed for mocking — its dependencies are) |
| 3.5 | Unit tests — Infrastructure | DONE | `JsonServiceTypeProviderTests` (against a temp JSON file, not the real seed file — decouples the test from another project's file layout), `MockTechnicianServiceTests`/`MockServiceBayServiceTests` — 5 tests. Repository-level DB tests (unique-constraint enforcement) deliberately live in Integration instead — that's a real-DB concern, not a unit-testable one |
| 3.6 | Integration tests — booking + concurrency/double-booking edge cases | DONE | `AppointmentBookingTests` (13 tests) via `SchedulerApiFactory : WebApplicationFactory<Program>`, one isolated temp SQLite file per test class instance (migrated via a standalone `DbContext` in the factory's constructor — see 2.5.4 for why; cleaned up in `Dispose`). Covers: happy path, same-slot 409, outside-hours/Sunday 400, invalid-technician 400, empty-vehicle 400, same-customer-twice dedup, **8-way concurrent identical requests → exactly 1×201 + 7×409**, availability endpoint (busy/free), health check, correlation-id capture (`X-Correlation-Id` echoed back verbatim) and auto-generate (fresh GUID minted when the header is absent) |
| 3.7 | Coverage reporting setup | DONE | `dotnet-reportgenerator-globaltool` pinned as a local tool (`.config/dotnet-tools.json`); README documents `dotnet tool restore` + `reportgenerator` commands (excludes ASP.NET Core's OpenAPI-generated code via `-classfilters`); CI runs the same generation step and uploads the HTML report as the `coverage-report-html` workflow artifact. No coverage-threshold gate — not requested, and a hard-coded % gate on a small assessment codebase would be more noise than signal |

**Checkpoint / output:** `dotnet test UnifiedSeviceScheduler.sln` → 77/77 passing (64 + 13), 0 failures, no leftover test artifacts (temp DB files cleaned up via `Dispose`). The concurrency guarantee that was only manually verified in earlier Task 2 sessions now has a real automated regression test.

---

## Task 4 — `README.md`

Status: **DONE** — full write-up, plus the actual deployment/CI artifacts it documents

| # | Item | Status | Notes |
|---|------|--------|-------|
| 4.1 | Build instructions | DONE | `dotnet restore`/`dotnet build` for the full solution |
| 4.2 | Run instructions | DONE | Local run, ports (5207/7048 per `launchSettings.json`), endpoints table, seeded `Dealership`, `serviceTypeCode` catalog table, `.http` file pointer, configuration table (connection string / `serilog.json` / OTLP endpoint), "Database Migrations" subsection (added earlier alongside 2.5.3) |
| 4.3 | Deploy — artifacts (VM, App Service) | DONE | VM: `dotnet publish` + example systemd unit file (non-root user, env-based config). App Service: `az webapp up` example. Both note config lives outside the artifact (env vars / App Service settings), not committed |
| 4.4 | Deploy — Docker container (+ future k8s note) | DONE | New `Dockerfile` (multi-stage SDK→ASP.NET runtime, non-root user, SQLite volume at `/app/data`, `ConnectionStrings__SchedulerDb` overridable via env for SQL Server in production) + `.dockerignore` + `docker-compose.yml`. **Build- and run-verified** (Docker daemon became available mid-session): `docker build` succeeds; container starts as non-root `appuser` (uid 5678); `/health` and `POST /appointments` both respond correctly against the running container; a booking made before `docker restart` was still queryable afterward, confirming the `/app/data` volume actually persists across restarts, not just that the container starts. `docker compose up --build` verified separately end-to-end too — kept, since it works (was conditional on that). All test containers/images/volumes cleaned up afterward. Future-Kubernetes paragraph ties to architecture.md §10's stateless-once-Redis scalability story |
| 4.5 | Test instructions | DONE | `dotnet test` for the full solution, 77-test breakdown (64 unit / 13 integration — includes the two correlation-id capture/auto-generate tests added later), gives a standalone `--filter` command for the concurrency-race test with a reviewer-checkable pass line, and documents the local + CI HTML coverage report workflow with an embedded screenshot (3.7) |
| 4.6 | GitHub Actions — run tests on PR create/update | DONE | New `.github/workflows/ci.yml` — `pull_request` (branches: main) + `push` (branches: main) triggers, restore/build/test, uploads `TestResults` (incl. `XPlat Code Coverage`) as a workflow artifact. YAML syntax-checked (`python3 -c "import yaml..."` — no GitHub Actions runner available here to fully execute it, same caveat as Docker) |
| 4.7 | Dependabot setup | DONE | New `.github/dependabot.yml` — weekly checks across `nuget`, `github-actions`, and `docker` ecosystems |
| 4.8 | AI Collaboration Narrative section | DONE | Written from this session's actual process, not generic boilerplate: doc-first/confirmation-gated design (with concrete examples of decisions that changed mid-session — Technician/ServiceBay externalization, concurrency mechanism, guest checkout), continuous self-verification (build+test+live curl smoke-testing), and a detailed walkthrough of the 2.5.4 `WebApplicationFactory` migration bug as a real example of root-causing rather than symptom-patching |

**Checkpoint / output:** `README.md` rewritten end-to-end; `Dockerfile`, `.dockerignore`, `docker-compose.yml`, `.github/workflows/ci.yml`, and `.github/dependabot.yml` created. Full solution build + `dotnet test` re-verified green (75/75 at the time; 77/77 as of the later correlation-id follow-up pass) after all additions — none of these new files affect the .NET build, confirmed rather than assumed.

**Follow-up pass — restructure + tone:** added a Table of Contents; regrouped Building/Running/Migrations/Testing under one "Getting Started" section, followed by "Deployment" then "CI/CD" (matches how a person would actually read this top to bottom, not the order sections happened to get written in). Added an explicit "A note on the database" callout: SQLite is for this assessment only, Azure SQL Database is the recommended production target. While writing that callout, caught and corrected an inaccuracy from the first pass — the README previously said switching to SQL Server needs "no code change," but `ServiceCollectionExtensions.cs` actually has `UseSqlServer` commented out next to the active `UseSqlite` call, so it's a one-line code change + rebuild, not connection-string-only. Fixed consistently everywhere the claim appeared (VM/systemd example, Docker section, the note itself).

**Follow-up pass — Azure Key Vault recommendation:** expanded architecture.md §6's "Secrets and connection strings" and added a matching README "Secrets and connection strings" subsection under Deployment (new TOC entry). Core recommendation: Managed Identity + Key Vault, specifically so the connection string never has to exist inside the CI/CD pipeline at all — App Service via a Key Vault reference app setting (a pointer, not a secret, safe in IaC), VM/container via `Azure.Extensions.AspNetCore.Configuration.Secrets` + `DefaultAzureCredential`. Explicit corollary: GitHub Actions secrets should hold deployment credentials (OIDC identity for `az login`), never the app's own connection string. While adding this, also tightened the existing VM/systemd and App Service deployment examples, which previously showed the raw connection string sitting directly in the unit file / implied it belongs in App Service's Configuration blade — both now point at the Key Vault approach instead of a plaintext value in a deploy-time config. Not implemented (no real Azure environment here) — documented as a recommendation per Agent.md's Security scope, consistent with the rest of §6.

**Follow-up pass — bug fix + coverage reporting (3.7) + Testing section rewrite:** while regenerating a real coverage report for this pass, `dotnet test` on a clean checkout showed 7/13 integration tests failing with SQLite `no such table: Dealerships`. Root-caused (not just re-run until green): `AddInfrastructureServices` (`ServiceCollectionExtensions.cs`) read `configuration.GetConnectionString("SchedulerDb")` into a local variable *before* calling `AddDbContext`, capturing that value into the options delegate's closure at service-registration time. `SchedulerApiFactory`'s `ConfigureAppConfiguration` override (which redirects the connection string to its isolated temp SQLite file) is applied by `WebApplicationFactory`'s minimal-hosting bootstrap *after* that point in `Program.cs`'s top-level execution, so the captured value was always `appsettings.json`'s default `Data Source=scheduler.db`, unmigrated relative to the test host's working directory — meaning `SchedulerApiFactory`'s own `Database.Migrate()` call was migrating a file the running app under test never actually used. Confirmed via an isolated repro (a scratch console app calling the same `Database.Migrate()` sequence standalone succeeded, proving the migration code itself was fine) before touching production code. Fix: read the connection string *inside* the `AddDbContext` options lambda instead of before it, so it's resolved lazily at DbContext-configuration time, after `IConfiguration` is fully assembled — one line moved, no behavior change for the real `Program.cs` startup path. Re-ran full suite: 77/77 green, including the concurrency-race test in isolation. Then: added `.config/dotnet-tools.json` pinning `dotnet-reportgenerator-globaltool` (closes 3.7); rewrote the README Testing section to give a reviewer an explicit, copy-pasteable command for running the concurrency test on its own with a result they can read directly, plus the coverage-report commands and an embedded screenshot (`docs/images/coverage-report.png`, generated headless and excluding ASP.NET Core's OpenAPI source-generated code via `-classfilters` so the numbers reflect code this project actually wrote — 94.7% line / 72.1% branch); wired the same report-generation step into `ci.yml` as a new `coverage-report-html` artifact so a reviewer never needs local tooling to see it.

**Follow-up pass — Testing section restructure + `.http` file overhaul:** split the Testing section into dedicated Unit Tests and Integration Tests subsections, each with a full summary table (test class/count/what it covers for unit tests, generated via `dotnet test --list-tests` rather than eyeballed, so the counts are exact — 10 classes summing to 64; test name/what it proves for the 13 integration tests) and its own `dotnet test <project>` command. Cross-referenced the two: Integration Tests now explicitly calls out that `Scheduler.Api.http` covers the same scenarios by hand, framed as a legitimate manual/exploratory complement to `dotnet test`, not a replacement for it. Rewrote `Scheduler.Api.http` itself: (1) replaced every hardcoded `startTime` with variables computed by a top-of-file script block — `businessDayIn(offsetDays)` rolls a base date past Sunday (dealership closed) and clamps the current time-of-day into 08:00–16:00, so every run targets a fresh, still-valid slot instead of a date that eventually lands in the past or collides with a slot booked by an earlier run against the same persisted `scheduler.db`; (2) added a "double-booking, sequential" demo pair (book once → 201, immediately rebook the identical slot → 409) runnable in any REST client; (3) added an 8-way "concurrent booking" section — 8 identically-slotted named requests, meant to be fired with `httpyac send --all --parallel 8` for a genuine (not merely close-together) simultaneous burst. This needed [httpyac](https://httpyac.github.io/) specifically, since the file's dynamic-variable script block used httpyac's own `{{ ... }}` JS-block syntax rather than a format any other tool recognizes — documented that incompatibility explicitly rather than let a reader discover it as a silent failure (unresolved `{{bookingStartTime}}` sent literally). *(Superseded below — that script syntax turned out to be the wrong choice for this project's actual reviewer environment.)* Also caught and fixed a real parsing bug along the way: the file's original convention of prefixing every line of a multi-line comment with `###` (not just the region divider) caused httpyac to treat each comment line as its own empty request region when run with `--all`, cluttering output with dozens of blank `===...===` headers; fixed by using a single `#` for comment continuation lines and reserving `###` for actual region dividers, verified by re-running the file end-to-end and confirming the request count matched exactly. Every command documented in this pass — the three `httpyac send` invocations, `dotnet run --project src/Scheduler.Api --launch-profile https`, the full sequential run, and the true parallel 8-way burst (confirmed via response-timing evidence, not just trusting the `--parallel` flag's name) — was actually executed against a running instance of the API before being written down, not assumed to work from reading httpyac's docs alone.

**Follow-up pass — corrected `.http` file to JetBrains-native syntax:** the previous pass's httpyac `{{ }}` script block broke immediately in the user's actual environment (JetBrains Rider, confirmed via running processes) with `Invalid URL` errors — Rider's built-in HTTP Client doesn't understand httpyac's proprietary block syntax at all, so it tried to parse the raw JS and the `#`-prefixed comment lines as literal request URLs. Root cause of the *design* mistake, not just the bug: the file was written for a generic "some CLI tool" audience instead of the reviewer's actual tooling. Fixed by rewriting the whole file in JetBrains' own native HTTP Client format instead of httpyac's: (1) added `src/Scheduler.Api/http-client.env.json` defining `HostAddress` per environment (`https`/`http`), replacing the in-file `@Scheduler_Api_HostAddress` variable — the idiomatic JetBrains way to select an environment (Rider's dropdown, or `--env`/`-e` from a CLI); (2) replaced the httpyac `{{ }}` block with a `< {% %}` pre-request script (JetBrains' own pre-request scripting syntax) attached to the "Health check" request, using `client.global.set(...)` instead of `exports.x =` — verified against JetBrains' own documentation (cross-referenced 3 separate doc pages: HTTP Client reference, response-handling examples, and a third-party walkthrough, since JetBrains' docs don't consolidate pre-request-script syntax in one place) and empirically confirmed working via httpyac, which turned out to also support this exact JetBrains dialect (httpyac bills itself as "the most interoperable implementation" — confirmed true here, not just marketing). Also added `ijhttp` (JetBrains' own official CLI, same execution engine as Rider's built-in client) as the primary "run without an IDE" answer ahead of httpyac, since it's the strongest possible compatibility guarantee for a JetBrains-using reviewer — httpyac is now positioned as the secondary tool needed only for the one thing `ijhttp` can't do, true parallel dispatch for the 8-way concurrency demo. Added `http-client.private.env.json` to `.gitignore` (JetBrains' convention for a secrets file some HTTP Client tooling auto-creates; not needed here since there are no secrets, but the file appeared during testing and shouldn't ever be committed if it reappears for someone else). Re-verified the entire corrected file end-to-end via httpyac against a live `dotnet run --launch-profile https` instance (all 17 requests, correct status codes throughout) before writing any of this down — same standard as the previous pass, not relaxed just because this was "only" a doc/tooling fix.

**Follow-up pass — Postman collection + AI Collaboration Narrative tone pass:** added `src/Scheduler.Api/Scheduler.Api.postman_collection.json` (Postman Collection v2.1) as a tool-agnostic alternative to the JetBrains-only `.http` file — same 17 requests across 3 folders (Happy Path & Validation, Double Booking Sequential, 8-Way Concurrent Booking), a collection-level pre-request script computing the same business-day/operating-hours-clamped times as the `.http` file's script (Postman's own `pm.collectionVariables.set(...)` API, not JetBrains' `client.global`), and `pm.test(...)` assertions on most requests so a Collection Runner pass gives a pass/fail summary. Generated via a Python script (not hand-written JSON) to avoid escaping mistakes in the request bodies, then verified for real: replayed all 17 requests against a live `dotnet run --launch-profile https` instance with the exact values the pre-request script would compute (cross-checked separately via `node -e`), confirming every status code matches what the collection's own test assertions expect. README's "Manual API testing" section restructured to present Postman as the tool most reviewers can use with zero setup, with the JetBrains `.http` file kept as a secondary option for anyone already in Rider/IntelliJ — this reverses the previous pass's implicit assumption that a JetBrains-native file alone was good enough for "manual testing" generally. Screenshots of an actual Postman run are expected from the user in a follow-up and will be added to the README once provided, rather than fabricated here.

**Follow-up pass — dropped `.http`/JetBrains entirely; dedicated Node.js concurrency script:** per direction, removed `Scheduler.Api.http` and `http-client.env.json` outright (not deprecated, deleted) rather than keeping them as a secondary option — the JetBrains-specific tooling built up over the last two passes (`ijhttp`, `httpyac`, `< {% %}` scripts) was more machinery than a reviewer should need to just see the app work. Replaced with two single-purpose tools instead of one do-everything file: the Postman collection (trimmed from 3 folders to 2 — dropped "8-Way Concurrent Booking (Manual)", since Postman's Collection Runner can't fire requests in parallel and clicking through 8 tabs by hand was always the weakest of the concurrency demonstrations built so far) stays the general-purpose manual/exploratory tool; a new standalone `scripts/concurrency-demo.js` takes over the concurrency demonstration specifically, using plain Node.js `fetch` + `Promise.all` — no framework, no CLI tool to install, `node scripts/concurrency-demo.js` and done. Regenerated the Postman collection via the same Python-script approach as the previous pass (for the same escaping-safety reason), re-verified all 9 remaining requests against a live instance.

The Node script went through a real bug during verification, not just a happy-path check: the first version reused a fixed Technician/Service Bay pair with only the target minute varying between runs, on the theory that a different minute means a different `AppointmentSlot` row. That's wrong — a 30-minute `OIL_CHANGE` occupies a 30-minute window (two 15-minute slot rows), so a second run one minute after the first still overlapped it and correctly got rejected with `409` for every one of the 20 requests, including what should've been the winner. Root-caused by hand (reproduced with a plain `curl` at a still-later minute, confirmed it also conflicted, read the actual response body instead of guessing) rather than assumed away. Fixed by generating a fresh random Technician/Service Bay pair (`crypto.randomUUID()`, built into Node 18+, no `uuid` package needed) on every run instead — since any non-empty GUID is accepted by the mocked validation, a fresh pair can never collide with a previous run's booking, and the date-juggling to dodge collisions became unnecessary. Verified by running the script three times back-to-back with no delay: three clean passes, exactly one `201` each time (and — good sign that the race is real, not an artifact of request-array ordering — the winning request number was different each run: #1, #1, #3).

Updated architecture.md's two remaining `Scheduler.Api.http` references (§6 Security's Correlation IDs discussion, §7 Observability's correlation-id capture/auto-generate example) to point at the Postman collection instead, so the design doc doesn't cite a file that no longer exists.

Also rewrote the AI Collaboration Narrative section for a plainer, more conversational voice (shorter sentences, less nested clause structure) at the user's request, keeping every concrete example and fact intact — this was a tone edit, not a content change. Added a Mermaid flowchart summarizing the write-brief → draft → review → verify → record loop described in prose throughout the section, since GitHub renders Mermaid natively in README files and a diagram makes the repeating loop easier to see than five paragraphs describing it.

**Follow-up pass — standard `ApiResponse` envelope for every API response:** at the user's request, every response from the Customer Booking API (not `/health` — a standard health-check convention endpoint, deliberately left alone) is now wrapped in one contract: `{ data, statusCode, message, errors[] }`, `errors` being `{ errorCode, errorMessage }` pairs so a single request that fails on multiple fields at once (FluentValidation doesn't cascade-stop across independent `RuleFor` chains) reports every failure in one response instead of one at a time. New types: `Scheduler.Api/Contracts/ApiResponse.cs` (`ApiResponse` + `ApiError` records) and `ApiResponseFactory.cs` (`Success`/`Failure` — the one place that assembles the envelope).

The DRY requirement was explicit and specific: the wrapping logic must live in exactly one place, not be copy-pasted per controller action. Implemented as two ASP.NET Core integration points, both calling the same factory rather than duplicating its logic: `Filters/ApiResponseWrapperFilter.cs`, a globally-registered `IAsyncResultFilter` (`AddControllers(options => options.Filters.Add<ApiResponseWrapperFilter>())`) that rewrites every controller `ObjectResult` — success payloads, `ProblemDetails` from `Problem(...)`, `ValidationProblemDetails` from FluentValidation *and* from ASP.NET Core's own automatic model-binding-failure 400 (verified this path too, by sending deliberately malformed JSON — it goes through `[ApiController]`'s built-in validation response, which the filter still catches generically, without any code written specifically for that case) — into the envelope; and `Middleware/ApiExceptionHandler.cs`, an `IExceptionHandler` (`AddExceptionHandler<ApiExceptionHandler>()`) for the one path the filter structurally cannot reach — an unhandled exception short-circuits past the MVC result pipeline entirely, straight into exception-handling middleware, before any result filter runs. `AppointmentsController` changed only to move its error responses from ad-hoc anonymous objects (`Conflict(new { error = ... })`) to `Problem(detail:, statusCode:, extensions: { ["errorCode"] = result.Status.ToString() })` — the framework-standard way to carry a machine-readable code alongside a human message, which `ApiResponseWrapperFilter` then reads generically.

Verified against a live instance, not just unit-level: manually inspected the real JSON for a success (201, `Appointment` under `data`), a business conflict (409, `errors: [{errorCode: "Conflict", ...}]`), a single business failure (400, `OutsideOperatingHours`), a multi-field validation failure (400, two entries in `errors`, one per invalid field), an availability check, and the malformed-JSON automatic-400 path — all six matched the contract exactly. Did not get to organically trigger a genuine unhandled 500 without modifying source to force one (decided against that — too invasive for a quick check); that path rests on `IExceptionHandler` being a standard, compiler-verified ASP.NET Core 8+ interface implementation rather than live-fire evidence, which is a narrower verification claim than everything else in this pass and is called out as such rather than glossed over.

Updated everything the response-shape change touched, not just the new code: `AppointmentBookingTests.cs` — fixed the tests that read response bodies (`ReadFromJsonAsync<AppointmentResponse>` → a new `ApiEnvelope<T>` test DTO wrapping it), including one, `CreateAppointment_SameCustomerTwice_ReusesCustomerId`, that had gone *silently* wrong rather than failing: with the old flat-body assumption still in the test but the new nested shape from the API, `CustomerId` deserialized to `Guid.Empty` on both sides and the equality assertion passed anyway by comparing two empty GUIDs — caught by inspection, not by a failing test, and fixed with an explicit `Assert.NotEqual(Guid.Empty, ...)` guard added so that regression class can't recur silently. Added one new test, `CreateAppointment_MultipleValidationFailures_ReturnsOneErrorPerField`, specifically for the multi-error scenario the envelope's `errors` array exists to serve — a request invalid on two fields at once, asserting both show up. Regenerated the Postman collection's `pm.test` assertions for the new envelope (`data`/`statusCode`/`errors[].errorCode` instead of flat-body checks) and re-replayed all 9 requests against a live instance to confirm every assertion actually passes, the same verification discipline as every other tooling pass in this log. `scripts/concurrency-demo.js` needed no changes — it only ever checked HTTP status codes, never response bodies.

Documentation: added `## 14. API Response Contract` to architecture.md (envelope examples, the DRY rationale, which files implement it) and a new "API Response Contract" section to Agent.md per the user's explicit request ("adding this skill into Agent.md") — the standing rule for this repo going forward, not just a description of what exists today. Added a "Response contract" callout to README's Endpoints table and updated the Integration Tests table for the new test and the 77→78 total. While cross-referencing architecture.md section numbers for these README edits, found and fixed several existing citations that had drifted from the document's actual numbering (e.g. README said "architecture.md §9 (Testing Strategy)" — actually §12; "§6 (Security)" — actually §9) — caught by systematically diffing every `§N` reference in README against a fresh, precise grep of architecture.md's real heading numbers rather than trusting the citations as written, after an initial re-check of my own that used a stale read and nearly re-introduced the same class of error while "fixing" it.

---

---

## Task 5 — Bounded-context refactor: Dealership as an internal service, Customer as a Value Object

Status: **IN PROGRESS** — `architecture.md` (design) updated and reviewed; code/tests/Postman/README not yet touched

Context: originally reasoned through as a "multi-tenant SaaS" framing (Dealership as tenant, a Provider Portal for
dealership staff), then simplified per user direction — keep the *outcome* (Dealership/Technician/Service Bay are
this platform's own internal services, not local Scheduler data) without the multi-tenancy vocabulary or complexity.

| # | Item | Status | Notes |
|---|------|--------|-------|
| 5.1 | Domain Assumptions: Dealership rewritten as internal-service-owned, not local data | DONE | `IDealershipProvider`/`MockDealershipProvider` pattern, same shape as Technician/ServiceBay |
| 5.2 | `Dealership.IsWithinOperatingHours(TimeRange)` moved off `AppointmentSchedulingPolicy` onto `Dealership` itself | DONE (doc) | Rationale: once Dealership crosses a bounded-context boundary, the operating-hours rule belongs to the type that owns the concept, not a Scheduler-side policy reaching into its fields. `AppointmentSchedulingPolicy` now only does `HasNoOverlap` |
| 5.3 | Repository interfaces moved to `Scheduler.Domain` (not `Scheduler.Application`) | DONE (doc) | New Architecture Principle #8: Repository = persistence for an aggregate this app owns → Domain. Provider = capability consumed from another bounded context → Application. Only `IAppointmentRepository` remains a Repository after 5.5 |
| 5.4 | `ITechnicianService`/`IServiceBayService` renamed to `ITechnicianProvider`/`IServiceBayProvider`; new `IDealershipProvider` | DONE (doc) | Matches existing `IServiceTypeProvider` naming. `INotificationService` stays a Service — it's an action (send), not a capability lookup |
| 5.5 | `Customer` redesigned from entity to Value Object owned by `Appointment` | DONE (doc) | Removes `ICustomerRepository`/`CustomerRepository`/`CustomerConfiguration`/`CustomerConflictException`/`UNIQUE(Email,Phone)` entirely — not renamed, deleted. `Appointment` embeds Name/Email/Phone directly (`OwnsOne`, same pattern as `Duration`). No shared `CustomerId` across appointments any more; a repeat customer is two independent rows with matching values, not a data-integrity concern |
| 5.6 | `Dealerships` table dropped from the schema entirely | DONE (doc) | `Appointment` + `AppointmentSlot` become the only two tables. Migration to drop `Dealerships` (and later, `Customers`) is a code-phase item, not yet generated |
| 5.7 | C4 L1/L2: Dealership/Technician/Service Bay reframed as **internal** services (same platform, different bounded context) inside an `Enterprise_Boundary`; Notification stays the one genuinely **external**, third-party service (e.g. SendGrid for email) | DONE (doc) | Was previously "external systems" for all four, which conflated "another bounded context we own" with "an actual third party" |
| 5.8 | API Gateway added to C4 L2 (documented, not implemented) | DONE (doc) | Sits on the `Scheduler API` → internal-services leg specifically (Dealership/Technician/Service Bay), not between the frontend and `Scheduler API` — that edge stays a direct call, unchanged. One gateway hop replacing three separate direct integrations once those services are real; also the natural home for the correlation-id-assigning edge component already discussed in Observability §10. Ties into the existing (renamed) "No API Gateway deployed" scope note rather than inventing new content |
| 5.9 | L3/L4 diagrams updated to match 5.1–5.6 | DONE | Also fixed a pre-existing drift in L4a: the old diagram showed `CustomerId` on `CreateAppointmentCommand` and Technician/ServiceBay deps directly on the Handler, but the actual code already had `CustomerName`/`CustomerEmail`/`CustomerPhone` and those deps live in `AppointmentAvailabilityChecker` — corrected while redrawing, not just re-skinned |
| 5.10 | Code: `Scheduler.Domain`/`Scheduler.Application`/`Scheduler.Infrastructure` changes to match the doc (interface moves/renames, `MockDealershipProvider`, `Customer` VO via `OwnsOne`, EF migration dropping `Dealerships`) | TODO | Not started |
| 5.11 | Update unit/integration tests for the new shapes | TODO | Mechanical rename for most (Moq setups on renamed interfaces); real rewrites for anything that asserted on `CustomerId`/shared identity or `IDealershipRepository` |
| 5.12 | Update `Scheduler.Api.postman_collection.json` | TODO | Response bodies change shape (`customer: {...}` instead of `customerId`); no new endpoints needed (dealership lookup isn't proxied through this API per user direction) |
| 5.13 | Update `README.md` | TODO | Seeded-dealership section, any `customerId` references, and the AI Collaboration Narrative section — the user moved `Agent.md` to `.agent/agent.md` and added `.agent/skills/ddd-cleanA-SOLID.md` (a DDD/Clean Architecture/SOLID skill reference used to ground this refactor's Repository-vs-Provider and Value-Object reasoning); the narrative should describe this change in workflow, not just the old single-`Agent.md` setup |

**Checkpoint / output:** `architecture.md` diff is ~415 insertions/~305 deletions across §1–§14 (every section that referenced the old Dealership/Customer/Provider shapes). Full read-through done section by section, mermaid fence count verified balanced (20, even) after each pass. No code, test, Postman, or README changes yet — those are 5.10–5.13, deliberately sequenced after doc review per user's explicit "update L3/L4 before we refactor completely" instruction.

## Open Decisions Needing User Input

None currently open.

## Resolved Decisions (for reference)

- `SharedKernel.*` namespace: resolved by omission — the Domain folder reorganization (2.5) moved `IAggregateRoot` into `Entities/` and the `ValueObject` base into `ValueObjects/` directly under `Scheduler.Domain`, so there's no separate `SharedKernel` namespace or project left to rename/extract.

- Database provider: **SQL Server is the production/target choice; SQLite is used for this assessment** (lightweight, no Docker needed). EF Core provider abstraction makes this a connection-string + `UseSqlite()`/`UseSqlServer()` swap. Concurrency design (`AppointmentSlot` unique-constraint ledger, not a Postgres-only range-exclusion constraint) is portable across both, so no behavior changes between demo and production. Docker is still needed for Task 4's containerized API deployment — that's independent of the DB choice.
- Technician/Service Bay: external systems, validated by id via `ITechnicianService`/`IServiceBayService` (mocked); Refit `I*HttpClient` interfaces defined for the real future integration but left empty/unwired for this assessment.
- Service Type: mocked via `IServiceTypeProvider`, backed by a static JSON file (not DB, not HTTP).
- Vehicle: no entity/master data; free text `"Make - Model - Trim/Variant+Year"` stored on `Appointment`.
- Dealership Staff/Manager: real actor with its own Staff/Admin API surface (broader scope — appointments across multiple customers), but that surface is **not implemented** in this assessment, only documented at L1/L2. This split must appear in L1/L2 and in the Security section, but **not** in L3/L4 (routing/authz concern, not a code-layer concern). *(Superseded: the original note here said the Customer Booking API scopes results to the requesting customer via access token — that assumed an authenticated flow later replaced by the guest-checkout redesign. The Customer Booking API is intentionally unauthenticated today; see architecture.md §6 Security > "Current state.")*
- Concurrency safety: double-booking prevented by a `UNIQUE(ResourceKind, ResourceId, SlotStart)` constraint on a new `AppointmentSlot` ledger table (15-minute slot granularity), not by the application-level read-check alone (that's a fast-fail UX optimization only — real guarantee is the DB constraint on insert).
